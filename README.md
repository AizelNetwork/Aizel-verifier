# Aizel Virifier Library

## Build and Test
cd verifier && cargo build && cargo test

The test case failure is expected because the jwt token is hard-coded in the test case. It has been out-dated. Please use the latest jwt token.
